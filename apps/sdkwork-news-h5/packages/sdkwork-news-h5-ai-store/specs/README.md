# Component Specs

This directory defines the local contract for `@sdkwork/news-h5-ai-store`.

The feature renders the touch-first AI Store and consumes an injected `AiStoreService`. AppStore, Skills, and MCP SDK composition remains in the application runtime.
