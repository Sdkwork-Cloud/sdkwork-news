# Component Specs

This directory defines the local contract for `@sdkwork/news-h5-account`.

The feature renders touch-first account states and consumes an injected `NewsAccountService`. IAM client construction and session persistence remain in the application runtime.
