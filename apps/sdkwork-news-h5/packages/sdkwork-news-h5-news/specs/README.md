# Component Specs

This directory defines the local contract for `@sdkwork/news-h5-news`.

The feature renders the paginated mobile feed and consumes an injected `NewsFeedService`. SDK construction, credentials, and endpoint selection remain in the application runtime.
