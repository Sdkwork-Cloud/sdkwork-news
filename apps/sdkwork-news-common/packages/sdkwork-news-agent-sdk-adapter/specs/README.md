# Component Specs

This directory defines the local contract for `@sdkwork/news-agent-sdk-adapter`.

The adapter maps the public Agents app SDK to the news agent profile and scheduling ports. It does not own credentials or client construction.
