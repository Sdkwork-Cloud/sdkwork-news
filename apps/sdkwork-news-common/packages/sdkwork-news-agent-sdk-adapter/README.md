# News Agent SDK Adapter

Stores news-reader profile metadata in SDKWork Agents manifests and consumes the composed
`@sdkwork/agents-app-sdk` facade. The news client does not fork agent persistence or API DTOs.
