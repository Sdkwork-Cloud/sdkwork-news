# Component Specs

This directory defines the local contract for `@sdkwork/news-ai-store-react`.

The controller owns query, cursor, command, and Skill artifact-selection state. It receives the capability service through injection.
