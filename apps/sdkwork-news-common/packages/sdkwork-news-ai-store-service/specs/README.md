# Component Specs

This directory defines the local component contract for `@sdkwork/news-ai-store-service`.

The service owns bounded Store queries and command validation. It has no SDK or React dependency.
