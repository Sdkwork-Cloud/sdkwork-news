# News Feed SDK Adapter Specs

Machine contract: `component.spec.json`.

This adapter consumes the composed `@sdkwork/news-app-sdk`, preserves server pagination, and exposes the `NewsFeedPort` required by `@sdkwork/news-feed-service`.
