# @sdkwork/news-feed-sdk-adapter

Adapter from the composed News App SDK to the cross-architecture news feed port. It never constructs clients, assembles headers, repairs HTTP envelopes, or downloads full lists for local pagination.
