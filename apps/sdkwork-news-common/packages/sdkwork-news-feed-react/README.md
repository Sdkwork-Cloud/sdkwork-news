# @sdkwork/news-feed-react

Shared React state controller for SDKWork News feeds.

`useNewsFeedController` owns channel selection, server cursor pagination, search, retry, and favorite command state. PC and H5 feature packages own their own rendering and responsive interaction.
