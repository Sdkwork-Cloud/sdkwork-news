# Component Specs

This directory defines the local contract for `@sdkwork/news-agent-contracts`.

The package owns platform-neutral agent profiles, conversation messages, scheduling values, and service port types shared by client architectures.
