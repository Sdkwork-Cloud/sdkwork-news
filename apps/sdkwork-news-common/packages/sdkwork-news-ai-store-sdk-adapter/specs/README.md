# Component Specs

This directory defines the local contract for `@sdkwork/news-ai-store-sdk-adapter`.

Only composed AppStore, Skills, and MCP SDK clients are accepted. The adapter never creates credentials or sends raw HTTP.
