# News Feed Service Specs

Machine contract: `component.spec.json`.

This component owns bounded news feed orchestration and its injected port. It does not construct SDK clients, read runtime configuration, render UI, or perform local full-list pagination.
