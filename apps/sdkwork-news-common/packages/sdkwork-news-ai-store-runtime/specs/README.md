# Component Specs

This directory defines the local contract for `@sdkwork/news-ai-store-runtime`.

The app root injects one `AuthTokenManager`; the runtime composes the AppStore, Skills, and MCP public SDKs behind the AI Store service.
