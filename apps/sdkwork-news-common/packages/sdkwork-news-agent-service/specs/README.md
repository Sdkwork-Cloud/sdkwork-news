# Component Specs

This directory defines the local contract for `@sdkwork/news-agent-service`.

The service composes the injected agent profile port and SDKWork IM conversation port into the Assistant capability exposed to PC and H5 features.
