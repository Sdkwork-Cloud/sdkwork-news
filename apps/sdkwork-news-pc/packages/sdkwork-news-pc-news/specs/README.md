# Component Specs

This directory defines the local contract for `@sdkwork/news-pc-news`.

The feature renders the PC feed and consumes an injected `NewsFeedService`. SDK construction, credentials, and endpoint selection remain in the application runtime.
