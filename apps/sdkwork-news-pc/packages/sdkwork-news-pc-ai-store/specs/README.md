# Component Specs

This directory defines the local contract for `@sdkwork/news-pc-ai-store`.

The feature renders the PC AI Store and consumes an injected `AiStoreService`. AppStore, Skills, and MCP SDK composition remains in the application runtime.
