# Component Specs

This directory defines the local contract for `@sdkwork/news-pc-account`.

The feature renders PC account states and consumes an injected `NewsAccountService`. IAM client construction and session persistence remain in the application runtime.
